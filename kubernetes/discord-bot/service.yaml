apiVersion: v1
kind: Service
metadata:
  name: discord-bot
  namespace: discord-bot
spec:
  type: ClusterIP

  selector:
    app: discord-bot

  ports:
    - name: bridge
      port: 3001
      targetPort: bridge
      protocol: TCP